import { DefaultTheme } from 'styled-components';
import {
  borders,
  colors,
  letterSpacings,
  radii,
  sizes,
  space,
  fontSizes,
} from './fields';

export const theme: DefaultTheme = {
  borders,
  colors,
  letterSpacings,
  radii,
  sizes,
  space,
  fontSizes,
};
